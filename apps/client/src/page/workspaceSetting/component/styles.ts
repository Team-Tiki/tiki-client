import { css } from '@emotion/react';
import { theme } from '@tiki/ui';

export const containerStyle = css({
  margin: '4.8rem 0',
});

export const shoolInfoTextStyle = css({
  color: theme.colors.gray_500,
});
