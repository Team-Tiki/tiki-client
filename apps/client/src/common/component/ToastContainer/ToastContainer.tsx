import { containerStyle } from '@/common/component/ToastContainer/ToastContainer.style';

const ToastContainer = () => <div css={containerStyle} id="toast-container" />;

export default ToastContainer;
